https://school.programmers.co.kr/learn/courses/30/lessons/42586


#앞 기능이 배포되어야 뒤 기능도 배포 가능 // 완료 순서 ≠ 배포 순서

#문제에서 구하는 것
“각 배포 시점마다 몇 개의 기능이 같이 배포되는가”
[2, 1] 첫번째 배포할 때는 2개 두번째 배포할 때는 1개

#“기준값 유지하면서 묶기” 패턴
1️⃣ 완료까지 걸리는 기간 계산
2️⃣ 앞 기준값으로 묶기

def solution(progresses, speeds):
    answer = []
    
    # 1️⃣ 각 기능이 완료되기까지 걸리는 일수 계산
    days = []
    for p, s in zip(progresses, speeds):
        remain = 100 - p
        day = remain // s
        if remain % s != 0:
            day += 1
        days.append(day)
    
    # 2️⃣ 배포 묶기
    current = days[0]
    count = 1
    
    for d in days[1:]:
        if d <= current:
            count += 1
        else:
            answer.append(count)
            current = d
            count = 1
    
    answer.append(count)
    
    return answer



   # 2️⃣ 배포 묶기 이해하기
###변수	역할
current	현재 배포 묶음의 기준 값
count	현재 묶음에 들어간 기능 개수
answer	각 배포에서 몇 개 배포되는지 기록

#  current = days[0]
첫 번째 기능이 끝나는 날을 기억해 둔다.

왜냐면
첫 번째 기능이 배포되는 날이 기준이 되기 때문.


# count = 1

의미

지금 배포될 기능이 하나 있다고 생각하고 시작한다.

왜냐면
첫 번째 기능은 이미 포함되어 있기 때문.


# for d in days[1:]:

days 리스트에서 두 번째 값부터 끝까지 하나씩 가져온다

이유
첫 번째 값은 이미 기준으로 사용했기 때문이다.

#if d <= current:
코드 의미

현재 기능의 완료일 d가
현재 배포 기준 current보다 작거나 같은지 확인

같으면
count += 1
같이 배포되는 기능 수를 증가

#else 의 의미: 현재 배포에 못 들어간다.

할일 두개
(1) 지금까지 묶인 기능 저장
answer.append(count)

= 현재 count 값을 결과에 추가

이유

반복문에서는 묶음이 끊어질 때만 저장
그래서 마지막 묶음은 아직 저장 안 된 상태로 끝난다.
따라서 마지막 묶음을 한 번 더 추가해야 한다.

(2) 새로운 배포 시작
current = d = 새 기준
count = 1 (새 묶음 시작)


