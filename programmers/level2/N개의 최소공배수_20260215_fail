from math import gcd
def solution(arr): 
    # x,y = 0,1 #동적 프로그래밍??
    a = []
    for i in range(0,len(arr)-1):
        a.append(gcd(arr[i],arr[i+1]))
    print(a)
    a=min(a)
    b=1
    for _ in arr:
        b *= _
    
    return b / a
