-- 코드를 작성해주세요
SELECT C.ID,  C.GENOTYPE,  P.GENOTYPE AS PARENT_GENOTYPE
FROM ECOLI_DATA AS C JOIN ECOLI_DATA AS P ON C.PARENT_ID=P.ID
WHERE (C.GENOTYPE & P.GENOTYPE) = P.GENOTYPE ########## 여기 몰랐음 =>부모의 형질을 모두 보유 조건 
ORDER BY C.ID ASC;
